[package]
name = "dashboard-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true

description = "Wire types shared by the Scufris dashboard server and widgets"

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"

[lints]
workspace = true
